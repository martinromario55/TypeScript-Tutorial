const superHeros: string[] = []
const heroPower: number[] = []
const heroWeapon: Array<string> = []

type User = {
  name: string
  isActive: boolean
}

const allUsers: User[] = []

// Arrays inside an array
const MLModels: number[][] = [
  [1, 2, 3],
  [4, 5, 6],
]

superHeros.push('spiderman')
heroPower.push(100)
heroWeapon.push('knife')

allUsers.push({
  name: 'John',
  isActive: true,
})
